{
  "name": "janseva-monorepo",
  "private": true,
  "scripts": {
    "app": "npm --prefix app run start",
    "server": "npm --prefix server run dev",
    "app:install": "npm --prefix app install",
    "server:install": "npm --prefix server install"
  }
}
